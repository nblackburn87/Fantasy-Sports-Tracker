<h1>Teams</h1>

<ul>
  <% @teams.each do |team| %>
    <li><a href="teams/<%= team.id %>"><%= team.name %></a></li>
  <% end %>
</ul>

<%= link_to "Add New Team", new_team_path, :class => "btn btn-default" %>
